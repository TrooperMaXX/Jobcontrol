<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.hoell.jobcontrol" >

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="de.hoell.jobcontrol.Start"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="de.hoell.jobcontrol.MainActivity"
            android:label="@string/title_activity_slidermenu" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

            </intent-filter>
        </activity>
        <activity
            android:name="de.hoell.jobcontrol.ticketlist.TicketDetailsActivity"
            android:label="@string/title_activity_ticket_details"
            android:parentActivityName="de.hoell.jobcontrol.MainActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="de.hoell.jobcontrol.MainActivity" />

        </activity>
        <activity
            android:name="de.hoell.jobcontrol.historie.Historie_Activity"
            android:label="@string/title_activity_historie">

            </activity>



    </application>

</manifest>
