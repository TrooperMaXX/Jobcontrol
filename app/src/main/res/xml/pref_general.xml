<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <CheckBoxPreference
        android:key="offline_checkbox"
        android:title="@string/pref_title_offline_starten"
        android:summary="@string/pref_description_offline_starten"
        android:defaultValue="false" />

    <!-- NOTE: EditTextPreference accepts EditText attributes. -->
    <!-- NOTE: EditTextPreference's summary should be set to its value by the activity code.>
    <EditTextPreference
        android:key="example_text"
        android:title="@string/pref_title_display_name"
        android:defaultValue="@string/pref_default_display_name"
        android:selectAllOnFocus="true"
        android:inputType="textCapWords"
        android:capitalize="words"
        android:singleLine="true"
        android:maxLines="1" />
    <de.hoell.jobcontrol.preference.NumberPickerPreference
        android:key="key_number"
        android:title="Updateintervall"
        android:defaultValue="55"
        / -->

    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:key="update_list"
        android:title="@string/pref_title_updateintervall"

        android:defaultValue="15"
        android:entries="@array/pref_update_list_titles"
        android:entryValues="@array/pref_update_list_values"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null" />


</PreferenceScreen>
